// sample(primary)
if        (T0 x = /* ... */; condition(x)) {
  // x is in scope here.
} else if (T1 y = /* ... */; condition(y)) {
  // x and y are in scope here.
} else {
  // x and y are in scope here.
}
// end-sample

